name: moodle_test_ag1
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.5.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: 1.0.8
  path_provider: ^2.1.4
  http: ^0.13.4
  flutter_dotenv: ^5.1.0
  mockito: ^5.4.4
  json_annotation: ^4.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  build_runner: ^2.4.8
  test: ^1.24.8
  json_serializable: ^6.8.0

flutter:
  uses-material-design: true
  assets:
    - assets/.env
